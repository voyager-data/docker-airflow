machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t voyagerdata/airflow .

test:
  pre:
    - sleep 5
  override:
    - docker run voyagerdata/airflow version |grep '1.7.0'
